{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma db push && node src/index.js",
    "restartPolicyMaxRetries": 5
  }
}
