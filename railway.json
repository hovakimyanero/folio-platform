{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate resolve --rolled-back 0001_init && npx prisma migrate deploy && node src/index.js",
    "restartPolicyMaxRetries": 5
  }
}
