# Backend API URL (used at build time by Vite)
# In development this is not needed — the Vite dev server proxies /api to localhost:4000.
# In production set this to the public backend URL, e.g.:
#   VITE_API_URL=https://folio-platform-production.up.railway.app/api
VITE_API_URL=
